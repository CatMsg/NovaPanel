# NovaPanel v1.6.76

## 修复

- 修复 Mieru 独立 `mita` 进程绕过面板路由规则和出站选择的问题。
- Mieru 流量现在通过仅监听本机回环地址的 SOCKS5 桥接进入 sing-box，支持面板的域名、IP、规则集、入站标签与默认出站规则。
- 修复桥接后 Mieru 入站流量可能被重复统计的问题，用户流量仍由 Mieru 独立计量。
- 修复修改 Mieru 监听端口、MTU 或内部出站后错误使用热重载的问题。

## 安全

- 内部路由桥接端口自动分配且只绑定 `127.0.0.1`，不会新增公网监听或防火墙放行规则。
