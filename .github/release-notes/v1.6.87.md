# NovaPanel v1.6.87

## MASQUE 健康检查修复

- 修复 MASQUE 服务端 90 秒空闲回收早于 OpenClash 180 秒健康检查，导致闲置节点被误判离线的问题。
- 无业务会话改为连续空闲 5 分钟后回收，确保周期健康检查可以复用有效会话。
- 持续活跃的连接和长时间传输不会被回收器中断。

## 验证

- 在 OpenClash 中强制选择 `la-masque` 与 `tk-masque`，HTTPS 请求均成功返回 `204`。
- 通过全部 Go 包测试、MASQUE Service Race 检测和 `go vet`。
