# NovaPanel v1.6.104

- Mieru 恢复为稳定的单端口监听与订阅，移除连续端口范围及相关转发逻辑；Hysteria2 连续端口保持不变。
- 规则目录默认只添加规则集，不再自动创建与默认出口相同的冗余路由；仅在明确选择出口或拒绝时创建流量规则。
- 规则目录支持同时选择 DNS 解析器，并自动合并重复的 DNS 规则集引用。
- DNS 页面专注解析服务器、默认解析器和缓存设置，DNS 分流规则统一迁移到路由列表管理。
- 路由与 DNS 规则卡片改为可读的匹配、目标和范围摘要，并保留排序、引用校验及重复配置保护。
