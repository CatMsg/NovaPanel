# NovaPanel v1.6.37

- 修复 MASQUE 节点服务端证书公钥与 OpenClash/mihomo `public-key` 校验不一致导致无法连接的问题。
- MASQUE 服务端新增 H3 CONNECT TCP 代理入口，适配 mihomo `h3-l4proxy` 模式。
- 新建 MASQUE 节点默认生成 OpenClash 可直接使用的 `h3-l4proxy` 配置，并关闭不支持的 UDP。
- MASQUE 状态页增加证书来源信息，便于区分节点私钥证书和文件证书。
