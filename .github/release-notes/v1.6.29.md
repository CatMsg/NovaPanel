# NovaPanel v1.6.29

## 新增

- MASQUE 节点新增独立状态查看弹窗，可直接查看运行状态、监听地址、证书路径和 URI 模板。
- MASQUE 新建和保存时自动回填当前机器 IP，去掉了手动填写 Server 的输入框。
- MASQUE mihomo 配置预览改为单行 `- { ... }` 样式，便于复制。

## 优化

- 修复 MASQUE 保存入口的自动回填逻辑，让空 Server 也能正常提交。
- 补齐 MASQUE 状态查询接口，便于面板中单独查看核心运行信息。
