## NovaPanel v1.6.44

- 修复 MASQUE `cf-connect-ip` 运行时模板 host 兼容问题，恢复使用 `cloudflareaccess.com`，避免 OpenClash / Mihomo 客户端请求被服务端错误拒绝。
- 保留上一版新增的 Mihomo/OpenClash 配置默认项，包括 `remote-dns-resolve: true` 和公共 DNS。
