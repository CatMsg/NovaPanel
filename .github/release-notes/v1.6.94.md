# NovaPanel v1.6.94

## 服务器集合移动端修复

- 修复服务器掉线、重启或更新状态提示出现后，手机端页面被长文本撑宽的问题。
- 状态提示改为卡片内自动换行，不再挤压顶部操作区域。
- 一键更新、一键重启、管理服务器和刷新状态按钮在窄屏下稳定保持两列布局并可正常点击。
- 收紧服务器卡片错误信息与页面容器宽度，避免横向溢出和内容裁剪。

## 验证

- 通过前端代码检查、类型检查和生产构建。
- 在 390px 手机宽度下验证顶部四个操作按钮完整位于屏幕内。
