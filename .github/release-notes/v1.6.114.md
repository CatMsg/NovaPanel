# NovaPanel v1.6.114

- 修复 Sing-Box 1.15 下远程规则集使用 `http_client.detour: direct` 导致核心无法启动的问题。
- 启动、保存和订阅输出前自动清理已有配置中的旧 `download_detour: direct` 与内联直连 detour，无需手工修改数据库。
- 规则目录、规则集编辑和批量导入统一改为留空表示直连，仅在选择真实代理出口时写入 detour。
- 清理 JSON 扩展模板中规则集下载的无效直连 detour，并补充旧格式、新格式和代理出口兼容测试。
