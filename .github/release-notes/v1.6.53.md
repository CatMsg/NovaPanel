# NovaPanel v1.6.53

## 新增
- MASQUE 节点新增 `SNI` 和 `Handshake Timeout`，OpenClash/mihomo 导出同步支持。
- MASQUE 新建/编辑时自动回填 SNI，减少手工配置失误。

## 修复
- 修正 MASQUE 保存时的 SNI 回填逻辑，避免 server 自动切换后仍保留空 SNI。
- 补齐 MASQUE 聚合与 Clash 转换的字段一致性。
