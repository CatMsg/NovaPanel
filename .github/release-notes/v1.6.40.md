## NovaPanel v1.6.40

- 修复 MASQUE 服务端 TUN 已建立但 UFW 默认 FORWARD DROP 导致 OpenClash 连接超时的问题。
- MASQUE 启动时自动配置 TUN 转发放行、回程连接放行、NAT 和 rp_filter，停止时同步清理规则，避免残留。
- 验证 OpenClash `tk-masque` 可完成 `generate_204` 延迟检测并返回可用状态。
