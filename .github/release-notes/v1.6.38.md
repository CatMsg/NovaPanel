## NovaPanel v1.6.38

- 修复 MASQUE 节点与 Mihomo/OpenClash 的协议不兼容问题，服务端改为 CONNECT-IP + Linux TUN/NAT 转发模式。
- MASQUE 节点配置恢复为 Mihomo 官方 `quic/h2` 形态，默认生成 IPv4 客户端地址并启用 UDP。
- MASQUE 证书继续使用节点密钥生成，确保 OpenClash `public-key` 校验与服务端证书匹配。
- 修正 MASQUE OpenClash 复制配置，避免生成不可用的 `h3-l4proxy` 配置。
