# NovaPanel v1.6.30

## 修复

- 修复 MASQUE 新建保存时报 `unexpected end of JSON input` 的问题，MASQUE 不再走 sing-box core 的 endpoint 增删流程。
- MASQUE 自动回填 server 现在优先使用公网 IP，避免回填成内网地址。
- endpoint 列表和 JSON 序列化对空或脏的 `options` 做了容错，避免一条坏记录影响整页刷新。

## 优化

- 保持 MASQUE 状态查看弹窗和 mihomo 单行预览逻辑不变，继续支持新建后直接复制使用。
