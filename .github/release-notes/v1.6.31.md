# NovaPanel v1.6.31

## 新增

- 节点管理保存时新增通用端口占用校验，端口与现有入站或其他节点冲突会直接拦截。
- MASQUE 新建默认端口改为随机值，保留手动修改能力。

## 修复

- 修复 MASQUE 使用 IPv6 host 时 URI 模板解析失败的问题。
- 修复节点管理里部分端口字段只在保存后才暴露冲突的情况。

## 优化

- 端口管理同步逻辑继续沿用统一脚本流程，入站、节点管理和面板端口更一致。
