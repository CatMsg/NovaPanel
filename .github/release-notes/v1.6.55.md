# NovaPanel v1.6.55

## 修复
- 为 MASQUE 订阅导出补充 `proto: bbr`，兼容 Shadowrocket 手动/订阅字段差异，同时保留 `congestion-controller: bbr` 供 Mihomo/OpenClash 使用。
- 继续保留 MASQUE 连接所需的 `sni`、`remote-dns-resolve`、`handshake-timeout` 等字段。
