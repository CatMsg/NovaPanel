# NovaPanel v1.6.80

## 服务器集合监测

- 为每台服务器增加 CPU 和内存占用监测，并以紧凑进度卡展示。
- 根据连续网络计数采样显示实时上传、下载速度。
- 页面可见时每 5 秒静默刷新，避免重复请求和操作弹窗期间的数据覆盖。
- 服务器详情同步展示资源用量和网络速率。
- 手机端监测卡使用 2 x 2 自适应布局，避免窄屏溢出。

## 兼容与验证

- 未升级的远端服务器会显示占位符，不会误报为零负载。
- 补充本机与远端资源载荷解析测试，并完成前端类型、代码规范和响应式页面验证。
