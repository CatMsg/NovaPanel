# NovaPanel v1.6.103

- Mieru 监听端口和连续公网端口范围放宽到 1-65535，低端口保留权限与端口冲突提示。
- Mieru 连续端口范围改为与 Hysteria2 一致的受管转发模型：核心仅监听稳定端口，公网范围由 NovaPanel 统一转发。
- Mieru 范围设置移入协议选项，公网范围可独立于监听端口配置，订阅继续输出 Mihomo `port-range`。
- 备份恢复同步支持低端口和独立 Mieru 公网范围，避免恢复时误判合法配置。
