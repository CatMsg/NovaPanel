# NovaPanel v1.6.117

- 服务器集合配置编排改为三步引导流程：先选择或创建模板，再明确选择目标服务器，最后核对逐台预览结果后确认下发。
- 目标服务器不再默认全选，切换模板时自动清空旧目标和灰度服务器，降低误操作风险。
- 模板范围、灰度服务器、执行顺序和预览状态采用独立卡片呈现，并完善桌面端与手机端布局。
- 修复配置预览按钮的缺失图标，并在每次打开编排时重置未保存的表单状态。
